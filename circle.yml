machine:
  node:
    version: v6

general:
  artifacts:
    - coverage

dependencies:
  cache_directories:
    - ~/.yarn-cache
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - yarn install

test:
  pre:
    - npm run lint -s
    - npm run flow -s
