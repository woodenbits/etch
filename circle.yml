machine:
  node:
    version: v6

general:
  artifacts:
    - coverage

dependencies:
  cache_directories:
    - ~/.cache/yarn
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.17.8
  override:
    - yarn install

test:
  pre:
    - yarn run lint
    - yarn run flow
